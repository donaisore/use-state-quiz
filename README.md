# useState Quiz

React の `useState` の挙動を理解するためのクイズアプリです。

Quiz0〜Quiz8 まで用意されており、それぞれ `useState` や `setState` のバッチ処理・再レンダリングに関する問題を出題します。各クイズでは画面右側にソースコードが表示されるので、コードを読みながら挙動を予想してみてください。

## セットアップ

```bash
npm install
npm run dev
```
